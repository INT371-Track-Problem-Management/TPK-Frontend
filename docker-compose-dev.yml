version: '3.3'
services:
  front-docker:
    build: .
    image: ghcr.io/artidsun/int371-frontend:latest
    container_name: frontendDev
    ports:
      - "8080:8080"
networks:
    default:
      external:
        name: project-net